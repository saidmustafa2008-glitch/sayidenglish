# Sayid English V4 — server environment variable NAMES only. Never put real values here.
# Configure real values in Vercel: Project → Settings → Environment Variables.
GROQ_API_KEY=
DEEPL_API_KEY=
GROQ_MODEL=
GROQ_SPEECH_MODEL=
